"""통합 웹 안에서 도는 내부 모듈이 놓이는 곳.

``<module_id>/routes.py`` 의 ``bp`` 와 ``<module_id>/panel.py`` 의 ``panel`` 이
기동 시 자동으로 등록된다. 계약은 ``application/module_assets.py`` 에 있다.

담당 WAS 를 따로 두는 원격 모듈은 여기에 아무것도 두지 않는다.
"""
